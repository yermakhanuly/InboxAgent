services:
  inboxagent:
    build: .
    restart: always
    env_file: .env
    volumes:
      - inbox_data:/data
    ports:
      # OAuth callback — must be reachable from your browser
      - "8080:8080"

volumes:
  inbox_data:
